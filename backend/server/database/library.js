var userName;
var password;
var upmail;

const LogIn = () => {
    // Enter Methods
}

const Register = () => {
    // Enter Methods
}

const LogOut = () => {
    // Enter Methods
}

// (Optional) Can be included if needed to check if item is an email
const verifyEmail = (upmail) => {
    // Enter Methods
}